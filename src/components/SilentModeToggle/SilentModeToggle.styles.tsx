import { StyleSheet } from 'react-native'

export const silentModeToggleStyles = StyleSheet.create({
  toggleContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    marginVertical: 10
  },
  toggleText: {
    fontSize: 20,
    fontWeight: 'bold',
    marginLeft: 10
  }
})
