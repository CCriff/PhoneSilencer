import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000', // You can choose a background color that matches the rest of the app
    padding: 10 // Add padding for consistent spacing around the app
  },
  title: {
    fontSize: 20,
    fontWeight: 'bold',
    marginBottom: 20
  }
})

export default styles
